# Prompt v1

- Uses a strict system prompt for Jon Klassen-inspired deadpan pacing.
- Requires JSON-only output from the model.
- Forces a 60-word max page and concrete next-word options.
- Keeps style guidance in `image_style_fingerprint` and scene guidance in `scene_description`.
